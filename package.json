{
  "name": "premium-mobility-foundation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "demo": "node examples/booking.ts",
    "check:docs": "node scripts/check-docs.mjs",
    "check": "npm run test && npm run check:docs"
  }
}
